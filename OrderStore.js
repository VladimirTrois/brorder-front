import { defineStore } from "pinia";

export const useOrderStore = defineStore("OrderStore",{
    //state
    state: () => {
        return {
            order,
        }
    }
    //actions

    //getters
})